# Copy to .env and fill in. Bun loads .env automatically (bun run serve),
# no dotenv package needed. .env itself is gitignored — never commit it.

ANTHROPIC_API_KEY=
# Everything below has a default; uncomment only to override.
# PUPPET_CHAT_MODEL=claude-sonnet-5
# PUPPET_CHAT_DAILY_USD=5
# PUPPET_CHAT_PER_IP=30
# PUPPET_CHAT_PER_SESSION=40
# PUPPET_CHAT_MAX_STEPS=30
